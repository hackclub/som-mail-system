/* 
 * This page needs to post the record_id to the api endpoint
 * "api/node_master_scan", then show a "success" message
 */

function NodeMasterScanPage() {
  return <div>Loading...</div>
}

export default NodeMasterScanPage